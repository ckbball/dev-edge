version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3030:3030"
    restart: unless-stopped
    networks:
      - default
    environment:
      HTTP_PORT: 3030
      DB_HOST: host.docker.internal:3306
      DB_USER: dev
      DB_PASSWORD: dev-user5
      DB_SCHEMA: team
